services:
  nginx:
    image: nginx:latest
    container_name: hermes-2api-nginx
    restart: always
    ports:
      - "${NGINX_PORT:-8088}:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - app
    networks:
      - hermes-net

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hermes-2api-app
    restart: unless-stopped
    env_file:
      - .env
    networks:
      - hermes-net

networks:
  hermes-net:
    driver: bridge
